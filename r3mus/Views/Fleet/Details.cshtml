@model r3mus.Models.Fleet

@{
    ViewBag.Title = "Fleet Details";
}

<div class="row">
    <div class="col-md-6">
        <h3>@Html.DisplayNameFor(model => model.Commander): @Html.DisplayFor(model => model.Commander)</h3>
    </div>
    <div class="col-md-6">
        <h3>@Html.DisplayNameFor(model => model.Time): @Html.DisplayFor(model => model.Time)</h3>
    </div>
    @foreach (var fComp in Model.FleetCompositions)
    {
        <div class="col-sm-3">
            @fComp.MemberName
        </div>
    }
</div>

<p>
    @Html.ActionLink("Back to List", "Index", null, new { @class = "btn btn-default" })
</p>
